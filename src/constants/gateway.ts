// Usage: Shared gateway constants mirrored from the Rust backend.

/**
 * Default gateway listen port, mirroring Rust DEFAULT_GATEWAY_PORT
 * (src-tauri/src/infra/settings/defaults.rs). Kept in sync by
 * src/constants/__tests__/crossLayerContracts.test.ts.
 */
export const DEFAULT_GATEWAY_PORT = 37123;

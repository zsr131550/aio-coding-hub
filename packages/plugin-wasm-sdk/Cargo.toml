[package]
name = "aio-plugin-wasm-sdk"
version = "0.1.0"
edition = "2021"
description = "Rust/WASM SDK contracts for AIO Coding Hub plugins"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
